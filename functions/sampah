# vim:et sts=2 sw=2 ft=zsh
#
# Unarchives files
#

CONFIG = "$HOME/.config"
if[ -d "$CONFIG/Code"]; then
    rm -rfv "$CONFIG/Code/Cache"
    ln -s /tmp/picobug "$CONFIG/Code/Cache"
fi
if[ -d "./node_modules" ]; then
    rm -rfv ./node_modules/**/**/.cache/**
fi